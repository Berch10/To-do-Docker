{
  "name": "todo-client-app",
  "description": "my first todo app",
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.7.2",
    "@types/bootstrap": "^5.2.10",
    "bootstrap": "^5.3"
  },
  "devDependencies": {
    "cpy-cli": "^5.0.0",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "build": "cpy 'public/**/*' 'build' && tsc --build && cpy node_modules 'build/'"
  }
}
